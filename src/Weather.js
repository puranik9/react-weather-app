import React, { Component } from 'react';

const Weather = (props) =>  {
    return (
        <div className="weather-dashboard">
            <h4>Your very own weather dashboard</h4>
        </div>
    )
};

export default Weather;